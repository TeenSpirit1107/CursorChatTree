import { ChatNode } from '../model/ChatNode';

/**
 * Placeholder for future Cursor chat storage parsing.
 * Will convert Cursor's internal chat format into ChatNode trees.
 */
export class ChatParser {
  parse(_raw: unknown): ChatNode | null {
    return null;
  }
}
